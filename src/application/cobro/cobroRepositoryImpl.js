
class CobroRepositoryImpl {
    guardarTransaccion(numeroTarjeta, codigoProducto, monto) {
        // Lógica para guardar la transacción en una base de datos
    }
}

module.exports = CobroRepositoryImpl;
