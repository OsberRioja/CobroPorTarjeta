
class CobroDTO {
    constructor(tokenLocal, codigoProducto, monto) {
        this.tokenLocal = tokenLocal;
        this.codigoProducto = codigoProducto;
        this.monto = monto;
    }
}

module.exports = CobroDTO;
